<div class="container py-3 mt-5">
  <div class="row">
    <div class="mx-auto col-sm-6">
      <!-- form user info -->
      <div class="card">
        <div class="card-header">
          <h4 class="mb-0">Editing Shop</h4>
          <%= link_to 'Show',  admin_shop_path(@shop), class: " btn btn-outline-primary pull-right show_btn" %> |
        </div>  
        <%= render 'form', shop: @shop %>
      </div>
    </div>
  </div>
</div>        


