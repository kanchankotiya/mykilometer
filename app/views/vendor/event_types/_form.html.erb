<%= form_with(model: [:vendor, event_type], local: true) do |form| %>
  <% if event_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_type.errors.count, "error") %> prohibited this event_type from being saved:</h2>

      <ul>
      <% event_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="card">
    <div class="card-header bg-secondary">
      <h4>Add Event Type</h4>
    </div>
    <div class="card-body">
      <div class="form-group ">
        <%= form.label :name %>
        <%= form.text_field :name, id: :event_type_name, class: "form-control" %>
      </div>

      <div class="form-group actions">
        <%= form.submit  class: "btn btn-secondary" %>
      </div>
    </div>
  </div>  
<% end %>
