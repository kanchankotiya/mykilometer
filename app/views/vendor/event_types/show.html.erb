<p id="notice"><%= notice %></p>
<div class="container mb-5">
  <p>
    <strong>Name:</strong>
    <%= @event_type.name %>
  </p>

  <%= link_to 'Edit',  edit_vendor_event_type_path(@event_type) %> |
  <%= link_to 'Back', vendor_event_types_path %>
</div>

